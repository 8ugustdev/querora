{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Querora window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
